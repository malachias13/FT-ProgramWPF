<Page x:Class="FT_ProgramWPF.View.DownloadingView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FT_ProgramWPF.View"
      xmlns:vm="clr-namespace:FT_ProgramWPF.ViewModel"
      d:DataContext="{d:DesignInstance Type=vm:DownloadingViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DownloadingView">
    <Grid>
        <Grid.Resources>
            <Style x:Key="DownloadPageButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Margin" Value="0, 25, 0, 0"/>
            </Style>
            <!--<Style TargetType="{x:Type Grid}">
                    <Setter Property="Margin" Value="0, 25, 0, 0"/>
                </Style>-->
            <Style TargetType="{x:Type Border}">
                <Setter Property="Margin" Value="0, 25, 0, 0"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                    HorizontalAlignment="Left"
                    Command="{Binding GetServerFilesCommand}"
                    MinHeight="30"
                    Margin="5,5,0,0">
            <TextBlock Text="Get Server Files"
                           Margin="0"
                           Padding="5"/>
        </Button>

        <Grid Grid.Row="1"
              Margin="0, 10, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Date"
                       Grid.Row="0"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>

            <TextBlock Text="Name"
                       Grid.Row="0"
                       Grid.Column="1"
                       VerticalAlignment="Top"/>

            <TextBlock Text="Download"
                       Grid.Row="0"
                       Grid.Column="3"
                       VerticalAlignment="Top"/>

        </Grid>

        <Border Grid.Row="2"
                MaxHeight="600"
                VerticalAlignment="Top"
                Margin="0,5"
                Background="Black"
                CornerRadius="5">
            
            <ScrollViewer Background="#212121"
              Margin="0, 1, 5, 1"
              VerticalScrollBarVisibility="Auto"
              MaxHeight="600">

                <ItemsControl ItemsSource="{Binding Files}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:Name="FileInfoTemplate">
                            <Border Padding="10"
                    BorderBrush="LightGray"
                    BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                               Grid.Row="0"
                               VerticalAlignment="Center"
                               Text="{Binding Data}"/>

                                    <TextBlock Grid.Column="1"
                               Grid.Row="0"
                               VerticalAlignment="Center"
                               Text="{Binding Name}" 
                               TextWrapping="WrapWithOverflow"/>

                                    <Button Command="{Binding DownloadCommand}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            Style="{StaticResource DownloadPageButton}"
                            Margin="0"
                            MinHeight="30"
                            Grid.Column="2">
                                        <TextBlock Text="Download"
                                   Margin="0"
                                   Padding="5"/>
                                    </Button>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>



        <Button Grid.Row="3"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Background="{Binding ServerToggleBtnColor}"
                Command="{Binding SeverToggleCommand}"
                IsEnabled="{Binding ServerToggleBtnEnable}"
                MinWidth="100">
            <TextBlock Text="{Binding ServerToggleTxt}"
               Margin="0"
               Padding="5"/>
        </Button>

    </Grid>

</Page>
